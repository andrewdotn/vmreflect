Maybe someday:
[ ] tcpr bug -- closing half a connection crashes the server

[ ] Use the VIX API instead of vmrun, because vmrun is way too slow.
[ ] Clean up the code by using nested with statements instead of finally.
[ ] Write custom multiplexing protocol and drop TcpProxyReflector.
